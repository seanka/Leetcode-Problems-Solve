class Solution {
    func reverseParentheses(_ s: String) -> String {
        var newChar: String = ""
        
        let stringArray = Array(s)
        let stringCount = s.count
        
        for i in 0..<stringCount {
            
        }
        
        return newChar
    }
}

let input: String = "(abcd)"
let sol = Solution()
print(sol.reverseParentheses(input))
